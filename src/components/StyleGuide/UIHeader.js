import styled from "styled-components";

const UIHeader = styled.header.attrs({
  className: "flex items-center p-10",
})`
  border-bottom: 1px solid #EEEEEE;
  font-size: 40px;
`;

export default UIHeader;
